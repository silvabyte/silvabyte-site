{
  "name": "silvabyte-site",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot ./index.ts",
    "build": "rm -rf dist && mkdir -p dist && bun build ./frontend.tsx --outdir ./dist --minify && bunx @tailwindcss/cli -i ./styles.css -o ./dist/frontend.css --minify && cp -r images dist/ && sed 's|./frontend.tsx|./frontend.js|; s|./styles.css|./frontend.css|' index.html > dist/index.html",
    "deploy": "bun run build && godeploy deploy --clear-cache"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.0",
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "tailwindcss": "^4.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
